[project]
name = "deepcube"
version = "0.1.0"
description = "Minimal infrastructure for reproducing DeepCubeA experiments."
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "torch>=2.0",
]

[tool.uv]
package = false
